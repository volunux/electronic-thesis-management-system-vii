let anchor = document.querySelectorAll('a');
let loadingBar = document.getElementById('aremi-loading-bar');
let mainMain = document.querySelector('main');

/*anchor.forEach((elm) => {

  elm.addEventListener('click' , function(e) {

  loadingBar.style.display = 'block';
  mainMain.style.display = 'none';

  e.stopPropagation();
  e.preventDefault();

  setTimeout(() => {

      location.href = e.target.href;
  } , 3000);

});


});*/  